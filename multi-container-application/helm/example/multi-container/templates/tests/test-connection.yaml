apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "multi-container.fullname" . }}-test-connection"
  labels:
    {{- include "multi-container.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "multi-container.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
